# คู่มือการตั้งค่าสำหรับอาจารย์ 👨‍🏫

## ขั้นตอนการเตรียมโปรเจ็กต์สำหรับการรัน

### 🔥 สำคัญ! ต้องสร้าง Firebase Project ก่อน

เนื่องจากโปรเจ็กต์นี้ใช้ Firebase เป็น Database และ Authentication จึงต้องมี Firebase Project ของตัวเองถึงจะรันได้

---

## 📋 ขั้นตอนที่ 1: สร้าง Firebase Project

### 1.1 เข้า Firebase Console

- เข้า [https://console.firebase.google.com](https://console.firebase.google.com)
- ล็อกอินด้วย Google Account

### 1.2 สร้าง Project ใหม่

1. กด **"Create a project"**
2. ตั้งชื่อ project เช่น `nd-report-system-test`
3. เปิด/ปิด Google Analytics ตามต้องการ
4. กด **"Create project"**

### 1.3 เปิดใช้งาน Authentication

1. ไปที่ **Authentication** > **Get started**
2. ไปแท็บ **"Sign-in method"**
3. เปิดใช้งาน **"Email/Password"**
4. กด **"Save"**

### 1.4 เปิดใช้งาน Firestore Database

1. ไปที่ **Firestore Database** > **Create database**
2. เลือก **"Start in test mode"** (สำหรับทดสอบ)
3. เลือก **Location** ใกล้ๆ เช่น `asia-southeast1`
4. กด **"Done"**

### 1.5 เพิ่ม Web App

1. ไปที่ **Project settings** (เฟืองตั้งค่า)
2. ไปแท็บ **"General"**
3. กด **"Add app"** > เลือก **Web (***</>***)**
4. ตั้งชื่อ app เช่น `ND Report Web`
5. **ไม่ต้อง** เลือก Firebase Hosting
6. กด **"Register app"**

### 1.6 คัดลอก Firebase Config

จะเห็นหน้าจอแสดง config แบบนี้:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com", 
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdefghijk"
};
```

**⚠️ เก็บข้อมูลนี้ไว้ จะใช้ในขั้นตอนถัดไป!**

---

## 📋 ขั้นตอนที่ 2: ตั้งค่าโปรเจ็กต์

### 2.1 ติดตั้ง Dependencies

```bash
npm install
```

### 2.2 ตั้งค่า Environment Variables

1. คัดลอกไฟล์ตัวอย่าง:

```bash
cp .env.example .env.local
```

1. แก้ไขไฟล์ `.env.local` ใส่ค่าจาก Firebase Config:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdefghijk
```

### 2.3 รันโปรเจ็กต์

```bash
npm run dev
```

### 2.4 เปิดบราวเซอร์

เข้า `http://localhost:3000`

---

## 📋 ขั้นตอนที่ 3: Setup ระบบครั้งแรก

### 3.1 ไปหน้า Setup

- เข้า `http://localhost:3000/setup`
- หรือคลิก **"ตั้งค่าระบบ"** ที่หน้าแรก

### 3.2 ทดสอบการเชื่อมต่อ

- กด **"เริ่มทดสอบ"** ในหน้าแรก
- รอให้ระบบตรวจสอบ Firebase
- ถ้าขึ้น ✅ แสดงว่าเชื่อมต่อสำเร็จ

### 3.3 สร้าง Admin User

- กรอกข้อมูล Admin:
  - **อีเมล**: `admin@test.com`
  - **รหัสผ่าน**: `123456`
  - **ชื่อ**: `ผู้ดูแลระบบ`
- กด **"สร้าง Admin User"**
- รอจนขึ้น **"ตั้งค่าเสร็จสิ้น!"**

---

## 📋 ขั้นตอนที่ 4: เริ่มใช้งาน

### 4.1 เข้าสู่ระบบ

- กด **"เข้าสู่ระบบ"**
- ใส่:
  - **อีเมล**: `admin@test.com`
  - **รหัสผ่าน**: `123456`

### 4.2 ทดสอบฟีเจอร์

✅ **ลองทำดู:**

- สร้างรายงานใหม่
- ดูรายงานใน Dashboard
- เปลี่ยนสถานะรายงาน
- สร้าง User ใหม่และทดสอบล็อกอิน

---

## ⚠️ หากมีปัญหา

### Firebase Connection ผิดพลาด

- ตรวจสอบ `.env.local` ว่าใส่ค่าถูกต้องหรือไม่
- ตรวจสอบว่าเปิด Authentication และ Firestore แล้ว
- ลองรีสตาร์ท `npm run dev`

### ไม่สามารถสร้าง User ได้

- ตรวจสอบ Firebase Authentication ว่าเปิดใช้ Email/Password
- ตรวจสอบ Firestore ว่าเปิดใช้ "test mode"

### ข้อมูลหาย

- ข้อมูลเก็บใน Firebase Firestore
- ดูได้ที่ Firebase Console > Firestore Database

---

## 📂 โครงสร้างที่สำคัญ

```text
📁 src/
  📁 lib/firebase/
    📄 config.ts         # ← การเชื่อมต่อ Firebase
  📁 app/
    📁 setup/           # ← หน้าตั้งค่าเริ่มต้น  
    📁 login/           # ← หน้าล็อกอิน
    📁 dashboard/       # ← หน้าหลักหลังล็อกอิน
📄 .env.local           # ← ไฟล์ config (ไม่อัปโหลด Git)
📄 .env.example         # ← ตัวอย่าง config
```

---

## 🎯 สิ่งที่จะได้หลังตั้งค่าเสร็จ

✅ ระบบแจ้งปัญหาพร้อมใช้งาน
✅ Admin สามารถจัดการรายงานได้
✅ User สามารถแจ้งปัญหาได้
✅ ข้อมูลเก็บใน Firebase แบบ Real-time
✅ ระบบ Authentication ที่ปลอดภัย

---

## 🚀 เสร็จแล้ว

ระบบพร้อมใช้งาน สามารถเริ่มสอนหรือ Demo ได้เลย

หากมีคำถามเพิ่มเติม สามารถดูโค้ดหรือถาม Developer ได้ครับ 😊
