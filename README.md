# 🏠 ND Report System - ระบบแจ้งปัญหาหมู่บ้าน/คอนโด

> ระบบแจ้งเรื่องออนไลน์ที่ทันสมัยและใช้งานง่าย สำหรับการแจ้งปัญหาในหมู่บ้านและคอนโดมิเนียม พัฒนาด้วย Next.js และ React

## 📑 สารบัญ

- [🎯 ฟีเจอร์ของระบบ](#-ฟีเจอร์ของระบบ)
- [👤 บัญชีทดสอบ](#-บัญชีทดสอบ)
- [🚀 การติดตั้งและรันโปรเจค](#-การติดตั้งและรันโปรเจค)
- [📁 โครงสร้างไฟล์](#-โครงสร้างไฟล์)
- [🛠 เทคโนโลยีที่ใช้](#-เทคโนโลยีที่ใช้)
- [🎨 การปรับแต่งระบบ](#-การปรับแต่งระบบ)
- [📝 การใช้งานและฟังก์ชัน](#-การใช้งานและฟังก์ชัน)
- [💾 การจัดการข้อมูล](#-การจัดการข้อมูล)
- [🌐 เส้นทางของระบบ](#-เส้นทางของระบบ)
- [📱 Responsive Design](#-responsive-design)
- [🔧 การพัฒนาและ Debug](#-การพัฒนาและ-debug)
- [📄 ลิขสิทธิ์](#-ลิขสิทธิ์)

## 🎯 ฟีเจอร์ของระบบ

### 👨‍💼 สำหรับผู้ดูแลระบบ (Admin)

| ฟีเจอร์ | คำอธิบาย | หน้าที่ใช้งาน |
|---------|----------|--------------|
| 🏠 **Admin Dashboard** | หน้าแรกแอดมิน พร้อมสถิติภาพรวม | `/dashboard` |
| 📊 **ดูสถิติ** | สถิติเรื่องแจ้งทั้งหมด รอรับเรื่อง กำลังดำเนินการ แก้ไขเสร็จ | Dashboard หลัก |
| 📋 **จัดการเรื่องแจ้ง** | ดู แก้ไข เปลี่ยนสถานะ และลบรายงานทั้งหมด | `/dashboard/reports` |
| 🔄 **เปลี่ยนสถานะ** | เปลี่ยนสถานะ: รอรับเรื่อง → กำลังดำเนินการ → แก้ไขเสร็จ | หน้ารายละเอียด |
| ➕ **แจ้งปัญหาใหม่** | สร้างรายงานใหม่ในนาม Admin | `/dashboard/create` |
| 🗑️ **ลบรายงาน** | ลบรายงานที่ไม่ต้องการ | หน้าจัดการรายงาน |
| ℹ️ **เกี่ยวกับเรา** | ดูข้อมูลทีมงานและโครงการ | `/dashboard/about` |

### 👤 สำหรับผู้ใช้ (User)

| ฟีเจอร์ | คำอธิบาย | หน้าที่ใช้งาน |
|---------|----------|--------------|
| 🏠 **User Dashboard** | หน้าแรกผู้ใช้ พร้อมสถิติส่วนตัว | `/dashboard/user` |
| ➕ **แจ้งปัญหาใหม่** | สร้างรายงานปัญหาในหมู่บ้าน/คอนโด | `/dashboard/create` |
| 👁️ **ดูเรื่องของฉัน** | ดูรายงานที่ตนเองแจ้งเท่านั้น | Dashboard ผู้ใช้ |
| 📊 **สถิติส่วนตัว** | สถิติการแจ้งของตนเอง | Dashboard ผู้ใช้ |
| 🗑️ **ลบรายงาน** | ลบรายงานของตนเองที่ไม่ต้องการ | Dashboard ผู้ใช้ |
| ℹ️ **เกี่ยวกับเรา** | ดูข้อมูลทีมงานและโครงการ | `/dashboard/about` |

### 🔒 ระบบ Authentication และ Authorization

| ฟีเจอร์ | คำอธิบาย | วิธีใช้งาน |
|---------|----------|----------|
| 🔐 **เข้าสู่ระบบ** | Login ด้วย Email และ Password | `/login` |
| 📝 **สมัครสมาชิก** | Register บัญชีใหม่ | `/register` |
| 👑 **Role-based Access** | แยกสิทธิ์ Admin และ User | อัตโนมัติเมื่อ login |
| 🚪 **ออกจากระบบ** | Logout และลบ session | เมนูด้านข้าง |
| 🛡️ **Route Protection** | ป้องกันการเข้าถึงหน้าที่ไม่มีสิทธิ์ | อัตโนมัติ |

## 👤 บัญชีทดสอบ

### 🔑 Admin Account (ผู้ดูแลระบบ)

```text
📧 Email: admin@test.com
🔒 Password: 123456
🎯 สิทธิ์: ดูและจัดการเรื่องแจ้งทั้งหมด
```

### 🔑 User Account (ผู้ใช้ทั่วไป)

```text
📧 Email: user@test.com  
🔒 Password: 123456
🎯 สิทธิ์: ดูและจัดการเฉพาะเรื่องของตนเอง
```

### 🔑 Test User Account (ผู้ใช้ทดสอบเพิ่มเติม)

```text
📧 Email: testuser@test.com
🔒 Password: 123456  
🎯 สิทธิ์: ผู้ใช้ทั่วไป
```

## 🚀 การติดตั้งและรันโปรเจค

### ✅ ความต้องการของระบบ

```bash
# ตรวจสอบ Node.js version (แนะนำ 18.0.0 ขึ้นไป)
node --version

# ตรวจสอบ npm version  
npm --version
```

### 📦 การติดตั้ง

```bash
# 1. Clone repository (หากมี) หรือเข้าไปในโฟลเดอร์โปรเจค
cd ND-report-system

# 2. เข้าไปในโฟลเดอร์ frontend
cd frontend

# 3. ติดตั้ง dependencies
npm install

# 4. รันเซิร์ฟเวอร์พัฒนา
npm run dev

# 5. เปิดเว็บไซต์ที่
# http://localhost:3000
```

### 🔧 Scripts ที่มีให้ใช้

```bash
# รันเซิร์ฟเวอร์พัฒนา (Development)
npm run dev

# สร้างไฟล์ production
npm run build

# รัน production server
npm start

# ตรวจสอบ code style (ESLint)
npm run lint
```

## 📁 โครงสร้างไฟล์

### 🗂️ โครงสร้างหลัก

```text
ND-report-system/
├── frontend/               # โครงการหลัก (Next.js)
├── backend/               # Backend API (ไม่ได้ใช้งานในเวอร์ชันนี้)
└── README.md             # คู่มือโครงการ
```

### 📂 โครงสร้าง Frontend

```text
frontend/
├── 📁 src/                    # ไฟล์ source code หลัก
│   ├── 📁 app/                # หน้าเว็บไซต์ทั้งหมด (App Router)
│   ├── 📁 components/         # Components ที่ใช้ร่วมกัน (ว่าง)
│   └── 📁 utils/              # Utilities และ Context
├── 📁 public/                 # Static files (รูปภาพ, ไอคอน)
├── 📄 package.json            # Dependencies และ scripts
├── 📄 next.config.mjs         # การตั้งค่า Next.js
├── 📄 tailwind.config.js      # การตั้งค่า Tailwind CSS
├── 📄 postcss.config.mjs      # การตั้งค่า PostCSS
├── 📄 eslint.config.mjs       # การตั้งค่า ESLint
├── 📄 jsconfig.json          # การตั้งค่า JavaScript
└── 📄 README.md              # คู่มือนี้
```

### 🗂️ โครงสร้าง app/ (หน้าเว็บไซต์)

```text
app/
├── 📄 page.js                 # 🏠 หน้าแรก (Landing page)
├── 📄 layout.js               # 🎨 Template หลักของเว็บไซต์
├── 📄 globals.css             # 🎨 CSS หลักของทั้งเว็บไซต์
│
├── 📁 login/                  # 🔐 ระบบเข้าสู่ระบบ
│   └── 📄 page.js
│
├── 📁 register/               # 📝 ระบบสมัครสมาชิก
│   └── 📄 page.js
│
└── 📁 dashboard/              # 🏢 ระบบจัดการหลัก
    ├── 📄 layout.js           # 🎨 Template ของ Dashboard
    ├── 📄 page.js             # 👨‍💼 หน้าหลัก Admin Dashboard
    │
    ├── 📁 user/               # 👤 หน้าสำหรับ User
    │   └── 📄 page.js
    │
    ├── 📁 create/             # ➕ หน้าแจ้งปัญหาใหม่
    │   └── 📄 page.js
    │
    ├── 📁 about/              # ℹ️ หน้าเกี่ยวกับเรา
    │   └── 📄 page.js
    │
    └── 📁 reports/            # 📋 หน้าจัดการรายงาน (Admin เท่านั้น)
        ├── 📄 page.js         # รายการรายงานทั้งหมด
        └── 📁 [id]/           # รายละเอียดรายงานแต่ละรายการ
            └── 📄 page.js
```

### 🔧 โครงสร้าง utils/ (ข้อมูลและฟังก์ชัน)

```text
utils/
├── 📄 authContext.js          # 🔐 จัดการ Authentication และ Authorization
├── 📄 reportsContext.js       # 📊 จัดการข้อมูลรายงานทั้งหมด  
└── 📄 helpers.js              # 🛠️ ฟังก์ชันช่วยต่างๆ (วันที่, สี, ฯลฯ)
```

## 🛠 เทคโนโลยีที่ใช้

### 🏗️ Framework และ Library หลัก

| เทคโนโลยี | เวอร์ชัน | หน้าที่ | เอกสาร |
|------------|---------|--------|---------|
| **Next.js** | 14.2.5 | React Framework สำหรับ SSR และ Routing | [nextjs.org](https://nextjs.org/) |
| **React** | 18.2.0 | JavaScript Library สำหรับ UI | [reactjs.org](https://reactjs.org/) |
| **Tailwind CSS** | 3.4.1 | Utility-first CSS Framework | [tailwindcss.com](https://tailwindcss.com/) |
| **Lucide React** | 0.460.0 | ไลบรารี่ไอคอนสวยๆ | [lucide.dev](https://lucide.dev/) |

### ⚙️ Development Tools

| เทคโนโลยี | เวอร์ชัน | หน้าที่ |
|------------|---------|---------|
| **ESLint** | 8 | ตรวจสอบคุณภาพโค้ด |
| **PostCSS** | 8 | ประมวลผล CSS |
| **Autoprefixer** | 10.0.1 | เพิ่ม vendor prefix |

### 🧩 การจัดการ State และข้อมูล

| ระบบ | ไฟล์ | หน้าที่ |
|------|-----|--------|
| **Context API** | `authContext.js` | จัดการ Authentication |
| **Context API** | `reportsContext.js` | จัดการข้อมูลรายงาน |
| **Local Storage** | Browser | เก็บสถานะ Login |

## 🎨 การปรับแต่งระบบ

### 🎨 ระบบสีและธีม

#### สีหลักของระบบ

```css
/* Primary Colors */
--indigo-50: #eef2ff;        /* พื้นหลังอ่อน */
--indigo-100: #e0e7ff;       /* พื้นหลังไอคอน */
--indigo-600: #4f46e5;       /* สีหลัก */
--indigo-700: #4338ca;       /* สีหลัก hover */

/* Neutral Colors */  
--slate-50: #f8fafc;         /* พื้นหลังหลัก */
--slate-600: #475569;        /* ข้อความรอง */
--slate-800: #1e293b;        /* ข้อความหลัก */

/* Status Colors */
--green-600: #16a34a;        /* สำเร็จ / เสร็จสิ้น */
--orange-600: #ea580c;       /* รอดำเนินการ */
--red-600: #dc2626;          /* ลบ / ยกเลิก */
--blue-600: #2563eb;         /* กำลังดำเนินการ */
```

#### การเปลี่ยนสีหลัก

1. **เปลี่ยนสีปุ่มหลัก** (`src/app/dashboard/*/page.js`):

```javascript
// เดิม
className="bg-indigo-600 hover:bg-indigo-700 text-white"

// เปลี่ยนเป็นสีอื่น
className="bg-purple-600 hover:bg-purple-700 text-white"  // สีม่วง
className="bg-emerald-600 hover:bg-emerald-700 text-white"  // สีเขียว
className="bg-blue-600 hover:bg-blue-700 text-white"      // สีฟ้า
```

1. **เปลี่ยนสีพื้นหลัง** (ไฟล์ต่างๆ):

```javascript
// เดิม
className="min-h-screen bg-slate-50"

// เปลี่ยนเป็น
className="min-h-screen bg-gray-50"     // สีเทา
className="min-h-screen bg-blue-50"     // สีฟ้าอ่อน
className="min-h-screen bg-green-50"    // สีเขียวอ่อน
```

### 🎯 การปรับแต่ง Component

#### การเปลี่ยนรูปลักษณ์การ์ด

```javascript
// เดิม - การ์ดธรรมดา
className="bg-white border border-gray-200 rounded-xl shadow-sm"

// เพิ่ม Shadow เข้ม
className="bg-white border border-gray-200 rounded-xl shadow-lg"

// เพิ่มสีพื้นหลัง
className="bg-gradient-to-r from-blue-50 to-indigo-50 border border-blue-200 rounded-xl shadow-sm"
```

#### การปรับแต่งเมนู

ไฟล์: `src/app/dashboard/layout.js`

```javascript
// เปลี่ยนสีเมนูที่เลือก
className={`... ${
  pathname === '/dashboard' 
    ? 'text-white bg-indigo-600 shadow-md'     // สีเดิม
    : 'text-white bg-purple-600 shadow-md'     // สีใหม่
}`}
```

### 📱 Responsive Breakpoints

```css
/* Tailwind CSS Breakpoints */
sm: 640px;    /* โทรศัพท์ขนาดใหญ่ */
md: 768px;    /* Tablet */
lg: 1024px;   /* Desktop เล็ก */
xl: 1280px;   /* Desktop ใหญ่ */
2xl: 1536px;  /* Desktop ขนาดใหญ่มาก */
```

## 📝 การใช้งานและฟังก์ชัน

### 🔐 ระบบ Authentication

#### การทำงานของระบบ Login

1. **ผู้ใช้กรอกข้อมูล** → `email` และ `password`
2. **ระบบตรวจสอบ** → เปรียบเทียบกับข้อมูลใน `authContext.js`
3. **สร้าง Session** → เก็บข้อมูลใน `localStorage`
4. **Redirect** → นำทางไปยังหน้าที่เหมาะสม

#### โค้ดตัวอย่าง Authentication

```javascript
// ใน authContext.js
const users = [
  {
    id: 1,
    name: 'Admin User',
    email: 'admin@test.com',
    password: '123456',
    role: 'admin'
  },
  {
    id: 2, 
    name: 'Regular User',
    email: 'user@test.com',
    password: '123456',
    role: 'user'
  }
]

// ฟังก์ชัน login
const login = (email, password) => {
  const user = users.find(u => u.email === email && u.password === password)
  if (user) {
    const userWithoutPassword = { ...user }
    delete userWithoutPassword.password
    setUser(userWithoutPassword)
    localStorage.setItem('user', JSON.stringify(userWithoutPassword))
    return true
  }
  return false
}
```

### 📊 ระบบจัดการรายงาน

#### สถานะของรายงาน

```javascript
const statuses = [
  'รอรับเรื่อง',        // สีส้ม - รอดำเนินการ
  'กำลังดำเนินการ',     // สีฟ้า - กำลังแก้ไข  
  'แก้ไขเสร็จ'         // สีเขียว - เสร็จสิ้น
]
```

#### ระดับความสำคัญ

```javascript
const priorities = [
  'ต่ำ',              // สีเขียว
  'ปานกลาง',          // สีเหลือง
  'สูง',              // สีส้ม
  'เร่งด่วน'          // สีแดง
]
```

#### หมวดหมู่ปัญหา

```javascript
const categories = [
  'ปัญหาน้ำ',         // น้ำรั่ว, น้ำไม่ไหล, ฯลฯ
  'ปัญหาไฟฟ้า',       // ไฟดับ, สวิตซ์เสีย, ฯลฯ
  'ปัญหาเสียง',        // เสียงดัง, รบกวน, ฯลฯ
  'เรื่องร้องเรียน',    // บริการ, พฤติกรรม, ฯลฯ
  'อื่นๆ'             // ปัญหาอื่นที่ไม่อยู่ในหมวดข้างต้น
]
```

### 🔄 การจัดการข้อมูล Realtime

#### สถิติแบบ Dynamic

```javascript
// ใน reportsContext.js
const getStats = () => {
  return {
    total: reports.length,
    pending: reports.filter(r => r.status === 'รอรับเรื่อง').length,
    inProgress: reports.filter(r => r.status === 'กำลังดำเนินการ').length,
    completed: reports.filter(r => r.status === 'แก้ไขเสร็จ').length,
    urgent: reports.filter(r => r.priority === 'เร่งด่วน').length
  }
}
```

## 💾 การจัดการข้อมูล

### 📄 โครงสร้างข้อมูลรายงาน

```javascript
// ตัวอย่างข้อมูลรายงาน
const report = {
  id: 1,                           // ID ไม่ซ้ำ
  title: 'น้ำรั่วจากเพื่อนบ้าน',      // หัวข้อปัญหา
  description: 'น้ำรั่วจาก...',      // รายละเอียดเต็ม
  category: 'ปัญหาน้ำ',             // หมวดหมู่
  status: 'รอรับเรื่อง',            // สถานะปัจจุบัน
  priority: 'สูง',                 // ระดับความสำคัญ
  date: '2024-11-25',             // วันที่แจ้ง
  createdBy: 'สมชาย ใจดี',          // ชื่อผู้แจ้ง
  contactEmail: 'somchai@test.com', // อีเมลติดต่อ
  contactPhone: '081-234-5678',    // เบอร์โทรติดต่อ
  location: 'ห้อง A101',           // สถานที่เกิดเหตุ
  additionalInfo: 'รีบด่วนมาก'      // ข้อมูลเพิ่มเติม
}
```

### 💾 การเก็บข้อมูลใน LocalStorage

```javascript
// เก็บข้อมูล user ที่ login
localStorage.setItem('user', JSON.stringify(userData))

// ดึงข้อมูล user
const userData = JSON.parse(localStorage.getItem('user'))

// ลบข้อมูลเมื่อ logout
localStorage.removeItem('user')
```

### 🔄 การเพิ่มข้อมูลตัวอย่าง

เปิดไฟล์ `src/utils/reportsContext.js` และแก้ไข `initialReports`:

```javascript
const initialReports = [
  // ข้อมูลเดิม...
  {
    id: Date.now(), // สร้าง ID ใหม่
    title: 'หัวข้อปัญหาใหม่',
    description: 'รายละเอียดปัญหา...',
    category: 'ปัญหาน้ำ',
    status: 'รอรับเรื่อง', 
    priority: 'ปานกลาง',
    date: new Date().toISOString().split('T')[0],
    createdBy: 'ชื่อผู้แจ้ง',
    contactEmail: 'email@example.com',
    contactPhone: '081-xxx-xxxx',
    location: 'ตำแหน่งที่เกิดเหตุ',
    additionalInfo: 'ข้อมูลเพิ่มเติม'
  }
]
```

## 🌐 เส้นทางของระบบ

### 🔓 หน้าสาธารณะ (Public Routes)

| Route | หน้า | คำอธิบาย |
|-------|------|----------|
| `/` | Landing Page | หน้าแรกต้อนรับ |
| `/login` | Login | เข้าสู่ระบบ |
| `/register` | Register | สมัครสมาชิก |

### 🔒 หน้าที่ต้อง Login (Protected Routes)

| Route | สิทธิ์ | หน้า | คำอธิบาย |
|-------|-------|------|----------|
| `/dashboard` | Admin | Admin Dashboard | หน้าหลักแอดมิน |
| `/dashboard/user` | User | User Dashboard | หน้าหลักผู้ใช้ |
| `/dashboard/create` | ทุกคน | Create Report | สร้างรายงานใหม่ |
| `/dashboard/reports` | Admin | Manage Reports | จัดการรายงานทั้งหมด |
| `/dashboard/reports/[id]` | Admin | Report Detail | รายละเอียดรายงาน |
| `/dashboard/about` | ทุกคน | About Us | เกี่ยวกับเรา |

### 🔄 Route Protection Logic

```javascript
// ใน layout.js
useEffect(() => {
  if (!loading && !user) {
    router.push('/login') // ไม่ได้ login -> ไป login
  } else if (user) {
    if (user.role === 'user') {
      router.push('/dashboard/user') // user -> ไป user dashboard
    }
    // admin อยู่หน้าเดิม
  }
}, [user, loading, router])
```

## 📱 Responsive Design

### 📐 Breakpoint และการใช้งาน

```javascript
// Grid ที่ปรับตามหน้าจอ
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"
//         โทรศัพท์: 1 คอลัมน์
//         Tablet: 2 คอลัมน์  
//         Desktop: 4 คอลัมน์

// ซ่อน/แสดงตามขนาดหน้าจอ
className="hidden md:block"        // ซ่อนในโทรศัพท์
className="md:hidden"              // แสดงเฉพาะในโทรศัพท์

// ขนาดข้อความที่ปรับได้
className="text-xl md:text-2xl lg:text-3xl"
```

### 📱 Mobile-First Design

1. **Navigation** - เปลี่ยนจาก sidebar เป็น mobile menu
2. **Cards** - ปรับขนาดและ padding
3. **Typography** - ลดขนาดตัวอักษรในมือถือ
4. **Buttons** - เพิ่มขนาดสำหรับ touch screen

## 🔧 การพัฒนาและ Debug

### 🐛 การแก้ปัญหาที่พบบ่อย

#### 1. หน้าขาวหรือ Error หลัง Build

```bash
# ลบ cache และ build ใหม่
rm -rf .next
npm run build
npm start
```

#### 2. Local Storage Error

```javascript
// ตรวจสอบว่าอยู่ใน browser หรือไม่
if (typeof window !== 'undefined') {
  localStorage.setItem('key', 'value')
}
```

#### 3. Context ไม่ทำงาน

ตรวจสอบว่า component อยู่ใน Provider หรือไม่:

```javascript
// ใน layout.js ต้องมี
<AuthProvider>
  <ReportsProvider>
    {children}
  </ReportsProvider>
</AuthProvider>
```

### 📊 การตรวจสอบ Performance

```bash
# วิเคราะห์ bundle size
npm run build
npx @next/bundle-analyzer
```

### 🔍 Development Tools

```javascript
// เปิด React Developer Tools ใน browser
// ดู Context values, Component tree, และ Props

// Console debugging
console.log('Current user:', user)
console.log('All reports:', reports)
console.log('Router pathname:', pathname)
```

### 🚀 การ Deploy

#### Vercel (แนะนำ)

```bash
# ติดตั้ง Vercel CLI
npm i -g vercel

# Deploy
vercel

# หรือ push ไปยัง GitHub และเชื่อม Vercel
```

#### Netlify

```bash
# Build static files
npm run build

# Upload build files ไปยัง Netlify
```

### 🔧 Environment Variables

สร้างไฟล์ `.env.local`:

```bash
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_APP_NAME=ND Report System
```

ใช้งาน:

```javascriptjavascript
const apiUrl = process.env.NEXT_PUBLIC_API_URL
```

## 🎓 สำหรับการนำเสนอและการเรียน

### ✨ จุดเด่นของโปรเจค

1. **🎨 Modern UI/UX** - ใช้ Tailwind CSS และ design patterns ทันสมัย
2. **🔐 Authentication** - ระบบ login และ role-based access control
3. **📱 Responsive** - ใช้งานได้ทุกอุปกรณ์
4. **⚡ Performance** - ใช้ Next.js SSR และ optimization
5. **🔧 Maintainable** - โครงสร้างโค้ดที่เป็นระเบียบ

### 📈 สิ่งที่เรียนรู้ได้

- **React Hooks** - useState, useEffect, useContext
- **Next.js App Router** - ระบบ routing แบบใหม่
- **Context API** - การจัดการ state แบบ global
- **Tailwind CSS** - utility-first CSS framework
- **Component Design** - การแยก component ให้เป็นระเบียบ

### 🛠 การพัฒนาต่อ

1. **เชื่อมต่อ Database** - MongoDB, PostgreSQL
2. **Real-time Updates** - WebSocket หรือ Server-Sent Events
3. **File Upload** - อัปโหลดรูปภาพปัญหา
4. **Email Notification** - แจ้งเตือนทาง email
5. **Mobile App** - React Native version
6. **การรายงาน** - สร้าง PDF reports
7. **Dashboard Analytics** - กราฟและสถิติเชิงลึก

## 📄 ลิขสิทธิ์

```text
ND Report System
Copyright © 2024 ND Development Team

โปรเจคนี้สร้างขึ้นเพื่อการศึกษาและสาธิต
ห้ามนำไปใช้เชิงพาณิชย์โดยไม่ได้รับอนุญาต
```

---

## 👥 ทีมงานผู้พัฒนา

| ชื่อ | บทบาท | Email |
|------|---------|-------|
| **ภูมิภูมินทร์ ธนภคินธยาน์** | Project Manager | `anuchit@nd-report.com` |
| **พัฒนดล นิโครธานนท์** | Full-Stack Developer | `somchai@nd-report.com` |
| **ภัทรวดี นวลตา** | UI/UX Designer | `niran@nd-report.com` |

---

## 🤝 การติดต่อและสนับสนุน

🌐 **เว็บไซต์:** [nd-report-system.vercel.app](https://nd-report-system.vercel.app)  
📧 **Email:** `support@nd-report.com`  
📞 **โทรศัพท์:** 02-123-4567

---

> 🎯 **หมายเหตุ:** โปรเจคนี้พัฒนาขึ้นเพื่อการศึกษาและสาธิตการทำงานของระบบแจ้งปัญหาออนไลน์ ในการใช้งานจริงควรเพิ่มระบบรักษาความปลอดภัย และเชื่อมต่อกับฐานข้อมูลจริง
